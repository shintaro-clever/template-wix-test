{
  "name": "figma-ai-github-workflow",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "postCreateCommand": "npm install && npm install -g @openai/codex",
  "remoteEnv": {
    "GITHUB_PAT_TOKEN": "${localEnv:GITHUB_PAT_TOKEN}"
  }
}
